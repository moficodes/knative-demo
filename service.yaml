apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: fib-knative-built
  namespace: default
spec:
  runLatest:
    configuration:
      build:
        apiVersion: build.knative.dev/v1alpha1
        kind: Build
        spec:
          serviceAccountName: build-bot
          source:
            git:
              url: https://github.com/IBM/fib-knative.git
              revision: vnext
          template:
            name: kaniko
            arguments:
            - name: IMAGE
              value: us.icr.io/mofi/fib-knative:latest
      revisionTemplate:
        spec:
          serviceAccountName: build-bot
          container:
            image: us.icr.io/mofi/fib-knative:latest
            imagePullPolicy: Always